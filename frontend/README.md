# Frontend

This directory hosts the CareFind Next.js application.

## Quick start

```bash
npm install
npm run dev
```

## Environment variables

Copy `frontend/.env.example` to `frontend/.env.local` and update the values.

## Scripts

- `npm run dev`
- `npm run build`
- `npm run start`
- `npm run lint`
